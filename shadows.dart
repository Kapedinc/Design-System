import 'package:flutter/material.dart';

class AppShadows {
  static const shadow = BoxShadow(
    color: Color(0x1a000000),
    offset: Offset(0, 1),
    blurRadius: 2,
    spreadRadius: 0,
  );

  static const shadow2 = BoxShadow(
    color: Color(0x0f000000),
    offset: Offset(0, 1),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const shadow3 = BoxShadow(
    color: Color(0x33000000),
    offset: Offset(0, 0),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const shadowL = BoxShadow(
    color: Color(0x12000000),
    offset: Offset(0, 4),
    blurRadius: 3,
    spreadRadius: 0,
  );

  static const shadowL2 = BoxShadow(
    color: Color(0x0f000000),
    offset: Offset(0, 2),
    blurRadius: 2,
    spreadRadius: 0,
  );

  static const shadowL3 = BoxShadow(
    color: Color(0x33000000),
    offset: Offset(0, 0),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const shadowXl = BoxShadow(
    color: Color(0x0a000000),
    offset: Offset(0, 10),
    blurRadius: 8,
    spreadRadius: 0,
  );

  static const shadowXl2 = BoxShadow(
    color: Color(0x1a000000),
    offset: Offset(0, 4),
    blurRadius: 3,
    spreadRadius: 0,
  );

  static const shadowXl3 = BoxShadow(
    color: Color(0x33000000),
    offset: Offset(0, 0),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const shadow2Xl = BoxShadow(
    color: Color(0x08000000),
    offset: Offset(0, 20),
    blurRadius: 13,
    spreadRadius: 0,
  );

  static const shadow2Xl2 = BoxShadow(
    color: Color(0x14000000),
    offset: Offset(0, 8),
    blurRadius: 5,
    spreadRadius: 0,
  );

  static const shadow2Xl3 = BoxShadow(
    color: Color(0x33000000),
    offset: Offset(0, 0),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const shadow3Xl = BoxShadow(
    color: Color(0x26000000),
    offset: Offset(0, 25),
    blurRadius: 25,
    spreadRadius: 0,
  );

  static const shadow3Xl2 = BoxShadow(
    color: Color(0x33000000),
    offset: Offset(0, 0),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const shadowS = BoxShadow(
    color: Color(0x0d000000),
    offset: Offset(0, 1),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const shadowS2 = BoxShadow(
    color: Color(0x66000000),
    offset: Offset(0, 0),
    blurRadius: 1,
    spreadRadius: 0,
  );

  AppShadows._();
}
